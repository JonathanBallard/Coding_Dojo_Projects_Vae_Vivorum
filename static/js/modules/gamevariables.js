


// ----------GAME VARIABLES----------

// GAME MAP SIZE
export var gameHeight = $(document).height() * 0.8;
export var gameWidth = $(document).width() * 0.7;
export var viewportHeight = $(document).height() * 0.8;
export var viewportWidth = $(document).width() * 0.7;

// SPEEDS
export var gameSpeed = 100;
export var playerMoveSpeed = 10;
export var enemyMoveSpeed = 10;
export var weaponFireMoveSpeed = 15;

//ENEMIES
export var enemiesKilled = 0;
export var numEscapedEnemies = 0;


//DAMAGE
export var impactDamage = 1000000;  //should 1shot everything for the moment, change to do partial damage later
export var missileDamage = 1000000; //should 1shot everything for the moment, change to do partial damage later












